# The measurement-arc capstone: the geometric object behind σ_z-sensitive readout, identified and classified (PR #241)

> **Framing.** QFT on the *fixed classical* throat geometry — geometry →
> fields, **not** quantum gravity. The capstone question, as posed:
> *identify the precise geometric object that would generate
> σ_z-sensitive readout; classify the result as derived geometry,
> additional detector structure, or imported measurement freedom.*
> Companion probe: ~18 s.

## 0. The question, made precise, and the answer

Probe K (#238) fixed what "σ_z-sensitive readout" means: the derived
setting is the fiber U(1) dial — *generated by σ_z* — and the derived
σ_z Stern–Gerlach commutes with it, so the readout statistics never
feel the dial (the abelian span{I, σ_z}; CHSH = 2 by commutativity).
Readout that **is** sensitive to the σ_z-generated dial requires the
non-abelian completion: the unique Δk = 2 mixer Probe L found (pure
σ_x, coefficient 1/√6) and Probe M validated (Z₈-conserving with a
winding-2 carrier; complete pointer statistics at Tsirelson). M left
one open item: **what geometry supplies the carrier.**

**The answer: an elliptic mouth.** A quadrupole (m = 2) deformation of
the mouth's fiber circle — equivalently, a coherently occupied
winding-2 mode of the throat field, whose mean field *casts* that
quadrupole. And the classification is not a choice among the three
categories but an **exact factorization across them**:

| component | classification |
|---|---|
| the operator (quadrupole, selection rule, coefficient, charge bookkeeping) | **derived geometry** |
| the occupation (the n̄ ≈ 16 condensate) | **additional detector structure** |
| the orientation (one U(1): the ellipse angle) | **the imported measurement freedom — located** |

## 1. The ellipse is the mixer, in closed form

Deforming the mouth fiber circle R(χ) = R₀(1 + ε·cos(2χ + φ₀))
produces exactly the missing generator:

```
⟨+1| H |−1⟩  =  2(1 − cos 2π/8)·ε·e^{iφ₀}  =  (2 − √2)·ε·e^{iφ₀}
```

on the committed N_χ = 8 fiber — linear in the ellipticity, **phase =
orientation** (both machine-checked; the multipole selection rule is
exact at O(ε): an m = 1 dipole or m = 3 deformation mixes Δk = 2 only
at O(ε²)). Only the quadrupole connects the k = ±1 qubit. This is the
geometric face of L's uniqueness result (Δk = m Fourier selection),
now stated as a *shape*: the mouth cross-section squeezed into an
ellipse.

## 2. The parity home: the sector matter cannot occupy

On the committed bridge barrier (`v_bridge_half`, r_s = 0.3), the
#202 parity theorem's two branches re-derived: odd modes have an
**exact node** at the neck (amplitude ~10⁻¹⁰, grid zero — the #221
committed near-neck law re-read), even modes have finite symmetric
neck amplitude (2×10⁻³–2×10⁻², contrast >10⁷). The winding-2 carrier
lives in the **even sector** — the sector the lepton ladder {1, 3, 5}
is parity-*excluded* from (#183/#221). The Bell apparatus and the
matter spectrum occupy complementary parity sectors of one throat
field: the carrier competes with nothing the matter sector uses.

## 3. The population scan — the honest negative

No committed mechanism populates the even winding-2 sector
*coherently*: the lepton ladder is odd by parity protection; #58 pair
creation makes ±1 winding pairs; the #223 dressing is |φ|²-driven and
phase-blind; vacuum occupation has no phase. The n̄ ≈ 16 condensate M
requires (back-action ledger re-read: n̄ = 1 → 2.29, 4 → 2.60,
16 → 2.76 against the ∞-carrier 2.82) is therefore **apparatus** —
the Stern–Gerlach magnet of this substrate: physical, geometric,
mouth-localized, but detector-supplied, not vacuum-supplied.

## 4. The phase is the dial

The qubit restriction of the quadrupole at orientation φ is

```
0.408248·(cos φ·σ_x − sin φ·σ_y)
```

— |coefficient| **constant** to 10⁻¹² (= 1/√6, matching L's ledger to
3×10⁻¹²), angle = −φ **exactly**, I/σ_z parts at machine zero — and
CHSH with the derived σ_z detector reaches **Tsirelson at every
orientation** (deviation 4×10⁻¹⁵ across the tested dial). No
orientation is preferred. The textbook measurement freedom — a Bloch
sphere of observable choices, imported as an axiom — reduces to **one
physical U(1) angle on one quadrupole at the detector mouth**.
Located, not eliminated.

The superselection reconciliation holds on the carrier model:
[H, K_total] = 0 exactly, the total-charge distribution is frozen
(3×10⁻¹⁶) while the reduced field state carries winding coherence
(0.027 at the tested coupling) — the #208 charged-GHZ no-go is
untouched.

## 5. The classification, assembled

- **Derived geometry** — the operator content: the even-parity
  winding-2 sector with mouth support (§2), the Δk = m selection rule
  making m = 2 unique (§1), the coefficient (1/√6 ledger;
  (2−√2)·ε closed form), the Z₈ bookkeeping (M re-read; total charge
  frozen).
- **Additional detector structure** — the occupation: the n̄ ≈ 16
  condensate (§3). Like asking a lab to carry a magnet: the
  requirement is physical and geometric, and it is the apparatus's to
  meet.
- **Imported measurement freedom** — the orientation: exactly one
  U(1) (§4). What the arc *eliminated*: all freedom in the operator
  content. What it *relocated*: the basis choice, from an axiom to a
  physical angle. What it *left open*: why any mouth carries the
  condensate — the same kind of question as why any lab carries a
  magnet.

The #238 trilemma is thereby factored, not evaded: the "one
non-derived ingredient" splits exactly as operator ⊗ occupation ⊗
orientation, each line labeled and machine-anchored.

## 6. Honest scope

- The models are the K/L/M qubit-level lattice models (the #206
  (x, χ) lattice), re-used and re-read — not the full 5D throat
  solve; the parity computation runs on the committed #223/#224
  bridge barrier.
- The ellipse-to-potential map is first order in ε; (2−√2) is the
  N_χ = 8 discrete-dispersion value.
- n̄ ≈ 16 is M's number, re-read, not re-derived. Whether throat
  collisions, accretion of even-sector quanta, or an engineered mouth
  deformation could supply the condensate is the open successor —
  this capstone classifies the requirement, it does not populate it.
- The condensate's phase stability during a measurement is the
  pointer-stability question of the #209/#219 sector.
- The classification concerns the Bell-chain readout; observables
  that never needed the quadrupole (energy, position) are unaffected.
- The #208 superselection no-go is reconciled, not weakened.

## 7. What would falsify this

- A Δk = 2 coupling from an m ≠ 2 deformation at O(ε) — the
  quadrupole would not be unique. (Checked: exact selection rule.)
- An orientation dependence of |coefficient| or a CHSH preference for
  some φ — the dial would carry hidden structure. (Checked: constant
  to 10⁻¹², Tsirelson everywhere.)
- An odd-sector mode with finite neck amplitude — the parity home
  would be wrong. (Checked: exact node, contrast 10⁷.)
- A committed source found to pump the even sector coherently — the
  occupation would move from detector structure to derived geometry,
  *strengthening* the program. (Scanned: none; the named successor.)
- Total-charge drift on the carrier model — the superselection
  reconciliation would fail. (Checked: 3×10⁻¹⁶.)

## 8. Companion probe

`experiments/closure_ledger/sigma_z_readout_capstone_probe.py`
(T1–T9, ~18 s): the K/L/M requirement re-read; the elliptic-mouth
closed form and selection rule; the parity home; the population scan;
the phase dial with Tsirelson at every orientation and the
superselection reconciliation; the assembled classification.

**Verdict:**
`THE_OBJECT_IS_AN_ELLIPTIC_MOUTH_THE_QUADRUPOLE_IS_DERIVED_GEOMETRY_ITS_OCCUPATION_IS_DETECTOR_STRUCTURE_AND_ITS_ORIENTATION_IS_THE_MEASUREMENT_FREEDOM_REDUCED_TO_ONE_U1_LOCATED_AT_THE_MOUTH`
