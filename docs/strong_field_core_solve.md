# The strong-field core solve: the collapse reading refuted, the anchor relocated (PR #210)

> **Framing.** QFT on the *fixed classical* throat geometry — geometry →
> fields, **not** quantum gravity. This PR executes #203's register
> target — the strong-field core contraction — at the tractable and
> sufficient level, and the refutation edge fires on the branch #203
> pre-registered. The companion probe measures everything (~3 min).

## 0. The target, and what "executing it" requires

#203 closed the mass-ladder thread onto one number:

> the strong-field core solve must yield a contraction
> r_q(weak)/r_s(true) = 13–45 for mouth pairing to land m_e/m_μ —
> failure mode: an O(1) contraction refutes the mechanism as the
> *quantitative* origin of the electron mass.

The adjudication does not need full dynamical numerical relativity. A
collapsing core passes through the **maximum-mass (turning-point)
configuration** of its static family, and the turning-point criterion
locates the instability; the critical configuration's scales bracket
the collapse endpoint's. So the sufficient computation is the **static
spherical GR family of the committed ψ–q structure** — the relativistic
completion of the locked #180 soliton — solved by shooting.

## 1. The solver, benchmarked

Static spherical Einstein–Klein–Gordon; ground state bracketed by the
0→1 node transition in the trial frequency; observables cut where the
field has decayed; M from the metric function. **The Kaup point is
reproduced: M_max = 0.6327 vs the classic 0.633 (0.2%), with
ω/m = 0.848 at criticality** (literature ≈ 0.85). The machinery
answers the canonical strong-field question correctly before being
asked anything new.

## 2. The committed structure, made relativistic — and the #179 runaway confirmed

The order field enters by adiabatic elimination (κ = 0.005 ≪ 1 in the
locked functional): q² = (gσ² − a₀)/λ above threshold, contributing the
non-convex effective potential

```
V_int(σ) = −(gσ² − a₀)²/(4λ)      (above the ordering threshold)
```

— the #179 runaway channel, relativistic. Measured (onset at
σ_c = 0.15): **the family's maximum mass sits at the ordering onset,
and the ordered branch loses mass monotonically** (M = 0.542 → 0.295)
— by the turning-point criterion the ordered branch is *unstable*:
switching on the core order destabilizes the star. The strong-field
endpoint of the committed structure **is** collapse — the premise of
#203's collapse reading, confirmed before the measurement that decides
its fate.

## 3. The measurement

At the collapse threshold, the wave-to-horizon ratio σ_mode/r_s
(r_s = 2M), across three potentials:

| family | M_max | RMS/r_s | R99/r_s |
|---|---:|---:|---:|
| Kaup (pure mass term) | 0.633 | 2.67 | 6.05 |
| **the committed q-channel** | 0.542 | 5.83 | 12.72 |
| repulsive-quartic control | 0.757 | 2.45 | 5.42 |

**Universally O(few–10)** — a factor ≥ 7 short of the required 88.6
(conv A), ≥ 16 short of 206.8 (conv B). The reason is structural, not
parametric: at criticality every length is the gravitational radius
times O(few) (the Buchdahl-type compactness bound). A configuration
with σ/r_s = 88.6 has compactness ~10⁻² — far from collapse, stable,
with nothing driving it to a horizon.

## 4. The verdict: the pre-registered failure mode fires

The collapse contraction available is
r_q(weak)/r_s(true) = **O(1)** (the weak-field band 4.6–6.7 that #203
measured overlaps the critical band 2.5–12.7) — not 13–45. Per the
failure mode #203 pre-registered, stated plainly:

> **The mouth-pairing mechanism is refuted as the quantitative origin
> of the electron mass in its collapse reading.** m_e stays
> over-predicted by an order of magnitude at any collapse endpoint.

The refutation is constructive — the committed dynamics really does
collapse its cores (§2); collapse just cannot make them light. What
survives, exactly as #203 stated: the smallness mechanism — the #195
index protection, the #201 multiplicative structure, the #202 exact law
and its naturalness.

## 5. The relocation: the primordial EM-capped throat

In BAM the throat was never a collapse product. It is the
**pre-existing topological object** — #168's regular 5D Tangherlini
Killing horizon, with the bulk mass μ = r_s² a geometric datum. And the
repo already anchors that radius: the **#55–#58 finite-self-energy
equilibrium** caps the throat's EM field at U_EM/(mc²) = α/2 — the
throat radius sits at the **classical electron radius** scale,
r_s ~ α·λ̄_C.

Under this reading the required #202 hierarchy becomes:

```
σ_mode/λ̄_C = 88.6 α = 0.647 (conv A)   /   206.8 α = 1.509 (conv B)
```

**The convention band brackets 1**: σ_mode = λ̄_C (the Compton cloud
#202 already described) and r_s = r_e meet the law **with no new
number**. Equivalently:

- the neck aspect **c = ln(σ/r_s) = ln(1/α) + O(1)**
  (4.484 = 4.920 − 0.436);
- **m_e/m_μ = (3/7 … 1)·α = [0.00313, 0.00730]** brackets the observed
  0.00484;
- the #203 contraction window is met by this anchor —
  r_q(weak)/r_s = [13.2, 44.6] ∈ [13, 45] — achieved by the primordial
  throat, not by collapse.

**Constrained, not derived**: the O(1) band is ×2.3 wide. But the mass
ladder's remaining unknown is now the *same α* that the #184 thread
protects as a boundary invariant — the electron/muon ratio as a
fine-structure phenomenon.

## 6. What is and is not established (honest scope)

**Established:** the solver (Kaup 0.2%); the #179 runaway as a genuine
GR instability of the committed structure; the critical σ/r_s band
2.5–12.7 across the potential class; the O(1) collapse contraction and
the refutation of the collapse reading; the α-anchor arithmetic and the
bracket of the observed ratio.

**Not established (the conditions):**
1. Static spherical 4D GR with the turning-point criterion — sufficient
   for the adjudication; full dynamical NR would sharpen the endpoint
   but cannot move it past the Buchdahl-type bound; 5D Tangherlini
   criticality differs by O(1) factors — unable to rescue a ×15
   shortfall.
2. Adiabatic q (κ = 0.005; percent-level gradient corrections).
3. Three potentials scanned; the structural compactness argument covers
   the class.
4. The α anchor inherits the #55–#58 scope (R* carries the program's
   one dimensionful anchor per B4/#184), and σ_mode/λ̄_C = O(1) is
   **constrained**, not derived.

**The register:** "do full 5D NR" is replaced by two sharper items —
derive the O(1) (σ_mode/λ̄_C), and connect the #55–#58 R* to the 5D
bulk mass μ = r_s². The mass ladder and the α thread are now one
thread.

## References

- D. J. Kaup, Phys. Rev. 172 (1968) 1331; R. Ruffini, S. Bonazzola,
  Phys. Rev. 187 (1969) 1767. [The boson star; M_max = 0.633 M_Pl²/m.]
- M. Colpi, S. L. Shapiro, I. Wasserman, PRL 57 (1986) 2485.
  [Self-interacting boson stars.]
- S. L. Liebling, C. Palenzuela, Living Rev. Relativity 26 (2023) 1.
  [Dynamical boson stars; the static system and stability.]
- H. A. Buchdahl, Phys. Rev. 116 (1959) 1027. [The compactness bound.]
- The BAM chain: #179/#180 (the runaway and the locked soliton), #202
  (the exact law), #203 (the confrontation and the pre-registered
  target), #168 (the primordial Tangherlini horizon), #55–#58 (the
  EM-capped equilibrium radius), #184 (α as protected boundary
  invariant).

## Reproduce

```bash
python -m experiments.closure_ledger.strong_field_core_solve_probe
# Verdict: COLLAPSE_READING_REFUTED_CRITICAL_SIGMA_OVER_RS_IS_O_FEW_NOT_88
#          _THE_ANCHOR_RELOCATES_TO_THE_EM_CAPPED_PRIMORDIAL_THROAT
#          _C_EQUALS_LN_ONE_OVER_ALPHA_PLUS_O1
```
