# `k_5 = 5` origin: `D_bulk = dim(S³) + 2`

Attempts the hardest piece of the lepton-sector structural arc — the
origin of the Tangherlini topological charge `k_5 = 5` itself. The clean
structural identification:

```
k_5  =  D_bulk  =  dim(time)  +  dim(radial)  +  dim(S³)  =  1 + 1 + 3  =  5
```

The BAM bulk is `time × radial × S³`, with `S³` the angular base of the
Hopf bundle (BAM's foundational primitive). `k_5 = 5` is then the BAM
bulk dimension; the chain reduces "why `k_5 = 5`" to "why the Hopf
bundle / S³ as the angular base" — the foundational BAM ansatz, established
across the spin/CPT arc (#59–#66, the spin-½ monopole, Wigner rotation,
CPT, throat Dirac spinor).

## Where every "5" in BAM comes from

All BAM Tangherlini-`D=5`-specific structure traces to `D_bulk = 5`:

| BAM feature | from `D = 5` | verified |
|---|---|:---:|
| `f(r) = 1 − (rs/r)²` | metric power `D − 3 = 2` | ✓ `V_tangherlini` source |
| `l(l+2)` centrifugal | S³ Casimir `l(l+D−3) = l(l+2)` | ✓ |
| `3·rs²/r⁴` throat curvature | coefficient `D − 2 = 3` | ✓ |
| `k_5 = 5` topological charge | `= D_bulk = dim(S³) + 2` | ✓ |
| `β_lepton = k_5²·(2π) = 50π` | quadratic face of `k_5` (#71) | ✓ |
| `#generations = (k_5+1)/2 = 3` | linear face of `k_5` (#72) | ✓ |
| `ε = 7π/(100·k_5⁴)` | inner-cutoff `k_5⁴` denominator (hbar_origin) | ✓ |

A single primitive (`D_bulk = 5 = dim(S³) + 2`) sits behind all of them.

## Why D=5 specifically (the "minimal squared" choice)

Tangherlini in `D` dimensions has `f(r) = 1 − (rs/r)^(D−3)`:

- `D = 4` (standard Schwarzschild): `f(r) = 1 − (rs/r)` (linear)
- `D = 5` (BAM): `f(r) = 1 − (rs/r)²` (**squared**)
- `D = 6`: `f(r) = 1 − (rs/r)³` (cubed)
- …

`D = 5` is the **minimal bulk dimension above 4D spacetime** giving a
"squared" metric — and that squaring is exactly the structure that
matches BAM's **spin-½ double cover** / `T² = −I` (B2): the throat
metric carries the same factor-of-2 / Z₂ partition as the spin
structure. The deeper structural reading: BAM lives at the minimal `D`
where the bulk-radial structure naturally hosts the spin-½ double cover
in its metric power.

## The chain (and where it stops)

```
Hopf bundle (BAM angular base, foundational)
 → S³  (dim = 3, the Hopf total space)
   → D_bulk = time + radial + dim(S³)  =  1 + 1 + 3  =  5
     → k_5 = D_bulk = 5
       → β_lepton = k_5²·(2π) = 50π          (#71)
       → #generations = (k_5+1)/2 = 3         (#72)
       → ε = 7π/(100·k_5⁴)                    (hbar_origin)
       → Tangherlini metric/centrifugal/curvature  (V_tangherlini)
```

`k_5 = 5` is **derived from `D_bulk = dim(S³) + 2`**. The remaining
input — the Hopf bundle as BAM's angular base — is the foundational
ansatz, established across the spin/CPT arc as the structure that gives
spin-½, the monopole, Wigner rotation, CPT, and the throat Dirac spinor
(#59–#66).

## Honest scope

  - **Is:** the structural identification `k_5 = D_bulk = dim(S³) + 2`,
    tying every BAM "5" to the bulk dimension; reduces the "why `k_5 =
    5`" question to "why S³ as the angular base" (the Hopf bundle, BAM's
    foundational primitive); identifies `D = 5` as the **minimal** bulk
    `D` whose Tangherlini metric is squared, matching the spin-½ double
    cover.
  - **Is not:** a fully first-principles derivation of "why the Hopf
    bundle / S³" from no assumptions at all. The Hopf bundle is BAM's
    foundational angular structure (set by the spin-½ requirement,
    independently established across the spin/CPT arc); this probe
    reduces `k_5 = 5` to that input but does not derive the input
    itself.

## B4 accounting

`k_5` (and `D_bulk`, `dim(S³)`) are dimensionless integers. The
structural identifications are topological — independent of the
dimensionful anchor `m_e`.

## Tests

  T1. **The Hopf bundle / S³ as the BAM angular base** (foundational
      primitive; recap from the spin/CPT arc, #59–#66).
  T2. **`k_5 = D_bulk = dim(S³) + 2 = 5`.** the bulk decomposition
      `time × radial × S³`.
  T3. **All four Tangherlini-D=5 features in `V_tangherlini`.** metric
      power, centrifugal, throat curvature, `k_5⁴` in ε.
  T4. **Why D=5 minimal: "squared" metric matches spin-½ double cover.**
      `D=5` is the minimal bulk above 4D spacetime giving
      `f(r) = 1 − (rs/r)²` (squared), matching the `T² = −I` /
      Z₂-partition structure (B2).
  T5. **Unification across the lepton sector.** same `k_5` primitive
      gives `β_lepton = k_5²·(2π) = 50π` (#71), `#generations =
      (k_5+1)/2 = 3` (#72), `ε = 7π/(100·k_5⁴)` (hbar_origin).
  T6. **The structural chain.** Hopf bundle → S³ → `D_bulk = 5 → k_5 =
      5 → β_lepton + #gen + ε + V_tangherlini`.
  T7. **Honest scope / B4.** reduces "why `k_5 = 5`" to "why the Hopf
      bundle" (foundational); identifies the structural form; not a
      fully assumption-free derivation. `k_5` dimensionless integer.
  T8. **Assessment.**

## Verdict structure

  - **K_5_FROM_BULK_DIMENSION** (expected): `k_5 = 5` is identified as
    `D_bulk = dim(S³) + 2`, the BAM bulk dimension. The chain reduces
    "why `k_5 = 5`" to "why the Hopf bundle / S³ as the angular base"
    (BAM's foundational primitive, established across the spin/CPT
    arc). `D = 5` is the minimal bulk above 4D spacetime giving the
    squared metric `f(r) = 1 − (rs/r)²` that matches the spin-½ double
    cover (`T² = −I`, B2). All BAM "5"s (`β_lepton = k_5²·(2π)`,
    `#generations = (k_5+1)/2`, `ε ∝ 1/k_5⁴`, the `V_tangherlini`
    coefficients) trace to this single primitive.

  - **NO_CHAIN**: the structural identifications do not match.

## What this leaves open

  - **The foundational Hopf-bundle ansatz.** `S³` as the angular base is
    BAM's foundational primitive (gives spin-½, the monopole, CPT, the
    throat Dirac spinor, etc.); a meta-argument for why nature picks the
    Hopf bundle would be philosophical/anthropic and is outside BAM's
    catalog.

## Cross-references

  - `docs/beta_lepton_derivation_research_plan.md` — `β_lepton =
    k_5²·(2π) = 50π` (#71); same `k_5`.
  - `docs/three_throat_modes_research_plan.md` — `#generations =
    (k_5+1)/2 = 3` (#72); same `k_5`.
  - `docs/hbar_origin_status.md` — `ε = 7π/(100·k_5⁴)`, the γ-lock.
  - `docs/topological_discrete_sector_research_plan.md` — `T² = −I` /
    Z₂-partition (B2).
  - `docs/cpt_dirac_operator_research_plan.md`,
    `docs/throat_dirac_spinor_research_plan.md` — the spin/CPT arc that
    establishes the Hopf bundle as the angular base.
  - `geometrodynamics/tangherlini/radial.py` — `V_tangherlini` source.
  - `experiments/closure_ledger/k5_origin_probe.py` — this probe.
